# AI 어시스턴트 활용 가이드

## IDE(예: Android Studio) 내에서 직접 실행할 수 있나요?
- 현재 제공되는 어시스턴트는 Android Studio 플러그인 형태로 IDE 내부에 "끼워" 넣을 수 없습니다.
- 대신 Git 변경 사항을 푸시/풀하면서 협업하는 방식으로 활용하거나, 터미널에서 요청한 작업(코드 수정, 리팩터링, 문서 보강 등)을 진행하도록 지시하는 방식으로 사용할 수 있습니다.

## 협업 흐름 예시
1. **브랜치 준비**: 원격 브랜치를 가져오고(`git fetch --all`) 작업 브랜치를 체크아웃합니다.
2. **작업 요청**: 구현/수정할 내용(화면, 로직, 테스트 등)을 어시스턴트에 설명합니다.
3. **코드 적용**: 어시스턴트가 코드 수정 및 커밋, PR 초안 작성까지 진행합니다.
4. **로컬 검증**: Android Studio나 에뮬레이터에서 `Run` 또는 `./gradlew assembleDebug && ./gradlew installDebug`로 직접 실행해 UI를 확인합니다.

## 수동으로 확인해야 하는 부분
- UI 렌더링, 에뮬레이터/실기기 동작 확인, 성능 측정 등은 IDE나 디바이스에서 직접 실행해야 합니다.
- 어시스턴트는 코드/리뷰/문서/테스트 작성까지 지원하지만, 실제 기기에서의 시각적 결과는 사용자가 직접 확인해야 합니다.

## 유용한 커맨드 모음
- 브랜치 확인: `git branch -a`
- 브랜치 체크아웃: `git checkout -b <새브랜치> origin/<기반브랜치>`
- 빌드: `./gradlew assembleDebug`
- 기기 설치: `./gradlew installDebug`
- 유닛 테스트: `./gradlew test`

필요한 작업이나 궁금한 점을 알려주시면, 코드 수정부터 문서 보강까지 도와드리겠습니다.
